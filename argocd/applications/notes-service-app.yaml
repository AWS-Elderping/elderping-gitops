apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: notes-service-app
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  project: healthcare
  source:
    repoURL: https://github.com/AWS-Elderping/elderping-gitops.git
    targetRevision: HEAD
    path: charts/notes-service
    helm:
      releaseName: notes-service
      valueFiles:
        - values.yaml
        - ../../environments/dev/notes-service-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: healthcare
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
