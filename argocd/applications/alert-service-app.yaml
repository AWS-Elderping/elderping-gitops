apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: alert-service-app
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  project: healthcare
  source:
    repoURL: https://github.com/AWS-Elderping/elderping-gitops.git
    targetRevision: HEAD
    path: charts/alert-service
    helm:
      releaseName: alert-service
      valueFiles:
        - values.yaml
        - ../../environments/dev/alert-service-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: healthcare
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
